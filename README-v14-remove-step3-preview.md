# v14 Step 3 preview cleanup

This patch removes the empty Step 3 selfie preview box. The Photo Booth now only shows the Take selfie and Choose photo actions in Step 3, and renders the selected selfie directly inside the generated trading card.

No Supabase changes are required.
